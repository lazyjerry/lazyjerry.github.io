請修復：

1. 「等級設定」、「支出設定」、「里程碑設定」、「收入設定」、「獎品設定」頁面的資料沒有正確帶入，編輯按鈕失效沒有帶出表單
2. 「收入管理」頁面點擊「執行」按鈕，應該會觸發 AJAX(請建立空的 funcion 模擬)，返回跳出成功/失敗訊息。並且「執行」按鈕文字應該是「確認到賬」。
3. 「獎品商店」頁面點擊「購買」按鈕，應該會觸發 AJAX(請建立空的 funcion 模擬)，返回跳出成功/失敗訊息。

---

## 請把 html 元素的 style 抽離出來成為 class，避免過多的 inline style 在元素之中。

請幫我整理 js 程式，我需要減少 js 渲染的 html 程式碼，是否能夠做成在 html 中建立一個隱藏的 template 區塊，js 要使用的時候在複製取用？這樣減少重複實現的程式碼。

實現後，請把 html 元素的 style 抽離出來成為 class，避免過多的 inline style 在元素之中。

---

操作記錄的頁面中，請把「遊戲幣變化」、「心情變化」、「經驗變化」，如果有變化的欄位，加上數值前後的變化資料，新添加欄位放在後方。並且確認 mockdata 是有提供該欄位資料的，這未來需要紀錄在資料庫中

---

請調整首頁的內容，注意手機版與電腦版皆需要正常的呈現：

1. 儀表板總覽下方數值呈現梗改為單行呈現： emoji + 名稱 + 數值 一行呈現。
2. 任務統計 卡片的標題與副標題改為一行呈現，並且讓這張卡片高度減少。
3. 當前稱號請改為滿版，並且除了現在的資訊，增加顯示統計：取得稱號日期、該稱號維持週數、該稱號維持日數。
4. 最新活動比照操作紀錄的呈現，以 table 呈現，顯示前 10 筆。欄位有時間、類型、描述、變化。
