/speckit.plan 專案 www/frontend 之中，使用 layui(已下載至：www/frontend/layui) 並且單純以 js/css/html 實現前端樣式，字體選用 google sans code

/speckit.plan 於 www/frontend 使用現有的前端技術，在不修改舊有的專案之中，實現需要的功能：

1. API 請共用 www/frontend/js/api/BaseAPI.js 結構，添加至 www/frontend/js/api 之中。
2. css 請獨立實現，添加至 www/frontend/css 之中，請使用與 game.html 相同的前端技術。
3. 建立 production dict 的 script 於 www/scripts 之中請獨立提供。
4. 確認當前所有使用到的 api 格式，依照現有框架建立 mockData 添加至 www/frontend/mock-data.js 之中。
5. 至目前為主串接使用到的 api 請參考 docs/www/frontend/guides/API 文件-OpenAPI.md 實現 openAPI json 檔案，建立於 www/swagger 之中。


/speckit.plan 於 www/frontend 使用現有的前端技術，在不修改舊有的專案之中，請參考與以下規格。

舊有的 API 文件：
1. docs/www/frontend/guides/API文件-OpenAPI.md 
2. www/swagger/auth-api.yaml

規格清單：
1. docs/www/frontend/guides/*
2. docs/Spec-Kit_實戰操作指南.md


/speckit.plan 於 www/backend/jerry/laravel/ 資料夾下，透過以下規格建立實現 www/swagger/auth-api.yaml 與 www/swagger/game-api.yaml 的 API 接口：
1. 使用 PHP Laravel 12 最新框架，所有需求儘量使用官方套件，或是最多人使用的套件
2. 透過 Laravel Sail 實現開發環境
3. 使用 MySQL 8.4, PHP 8.4 作為必要條件，其他有需要，例如 Redis 或是其他設定再另外安裝
4. 實現 XDEBUG 並且提供開發時使用，部屬正式環境下需要禁用
5. 實現 Telescope 並且提供使用，部屬正式環境下需要禁用
6. 實現 queue 以實作寄信功能（註冊成功、登入新 ip 通知、任務完成/里程碑完成等恭喜通知）
7. 提供 3 個說明書: 部屬（deploy.md）、開發(dev.md)、介紹（README.md）

/speckit.plan 於 www/backend/jerry/laravel/ 資料夾下，透過以下規格建立實現 www/swagger/auth-api.yaml 與 www/swagger/game-api.yaml 的 API 接口：
1. 請檢查前端專案程式碼 www/frontend 與 www/swagger/auth-api.yaml 與 www/swagger/game-api.yaml 這兩個 swagger 文件是否有落差，請參考 docs/research-skill/frontend-api-swagger-consistency-001/report.md 研究結果，如果有錯請依照前端程式碼為主修改 swagger 文件。
2. 檢查確認修改的 api 端點是否有錯誤與缺失，進行修復與添加。
3. 安裝並執行 PHPStan 分析，進行修復。
4. 安裝 Laravel Scramble 套件並且提供開發時使用，部屬正式環境下需要禁用
5. 更新 README 文件